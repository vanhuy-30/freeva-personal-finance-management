# Incident

1. Xác định: API down, data sai, leak PII, đối tác (Phase 6).
2. Mitigate: feature flag off, rotate token, scale, readonly.
3. Không xóa log. Che PII khi paste vào chat.
4. Thông báo user nếu dữ liệu tài chính sai hoặc breach (PDPD).
5. Postmortem ngắn trong `docs/project-state/CHANGELOG.md` hoặc issue `INF-*`.

Liên hệ on-call: TBD.
