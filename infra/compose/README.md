# Local dependencies

```bash
# from repo root
make up     # postgres :5434, redis :6379, mailhog UI :8025
make down
```

Credentials khớp `.env.example`: user/password/db `freeva`.

Không dùng compose này cho production.
