name: mobile
description: Freeva personal finance — Flutter iOS + Android
publish_to: "none"
version: 0.0.1+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get_it: ^8.0.3
  injectable: ^2.5.0
  go_router: ^14.6.2
  logger: ^2.5.0
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.13
  injectable_generator: ^2.6.2

flutter:
  generate: true
  uses-material-design: true
