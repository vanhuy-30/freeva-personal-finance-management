# @freeva/web-admin

Next.js App Router, port **3001**. Theme từ `@freeva/design-tokens`.

```bash
# repo root
make up && make api
make admin
```

Mở http://localhost:3001 — trang health. Auth staff = `WA-P0-002`.
