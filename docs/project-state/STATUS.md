# STATUS

- **Phase hiện tại:** 0 — nền tảng
- **Cập nhật:** 2026-08-26
- **Scaffold:** local health API + admin placeholder + Flutter shell (theme tokens, l10n, DI)

## Blocker

- Chưa có staging cloud, crash SaaS, region dữ liệu (xem DECISIONS-OPEN).

## Ưu tiên tuần

1. Review data model + security docs (`BE-P0-001`, `SEC-P0-001`).
2. ~~Chạy `make up` + `GET /api/health`~~ — local stack đã lên (`INF-P0-001`).
3. Persona / ToS draft (`PRD-P0-001`).

## Ghi chú

Foundation monorepo đã khởi tạo. Nghiệp vụ ví/giao dịch **chưa** làm.
