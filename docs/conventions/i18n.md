# i18n

- App user: `vi` mặc định, `en` thứ hai.
- ARB / Flutter gen-l10n. Admin: next-intl hoặc JSON `vi`/`en` (scaffold: một locale, sẵn cấu trúc).
- Không nối chuỗi số tiền bằng tay — formatter theo locale + minor units.
- Tìm kiếm: normalize không dấu (Phase 1 Should).
