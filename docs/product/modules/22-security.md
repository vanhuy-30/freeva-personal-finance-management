# Module 22 — Bảo mật

- **Phase:** 0 bắt buộc, 1 phát hành
- **Prefix:** `SEC`, `BE`, `MOB`

TLS + mã hóa lúc lưu (tối thiểu field nhạy cảm / disk). Secret/token. Băm mật khẩu, OTP/rate limit, thu hồi phiên. Che PII trong log. Threat model, audit thao tác quan trọng, quy trình báo cáo lỗ hổng. Khóa app, ẩn số dư nền.
